using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerStatusProgram : MonoBehaviour
{
    public int p_Hp = 100;
    public int p_SwordAttack = 10;
    public int p_BowAttack = 10;
    public int p_Defense = 10;
    public int p_AitemPoint = 0;
    public int p_CounterAttack = 10;

    public bool p_SwordAttackFlag = false;
    public bool p_BowAttackFlag = false;
    public bool p_CounterAttackFlag = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
